"""Dashboard API subpackage (s16).

The dashboard aggregates per-student progress metrics. Subrouters live
in ``eleve.py`` (s16) and, in later stories, ``parent.py`` (s17) and
``admin.py`` (s22+).
"""
