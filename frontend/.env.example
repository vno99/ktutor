# ktutor frontend — environment variables
#
# Backend API base URL. The CI job `frontend` injects this with the local
# backend address; locally, it points to the FastAPI on :8000.
NEXT_PUBLIC_API_URL=http://localhost:8000
